import generate from 'utils/common/ColorGenerator';

const Colors = [
  generate(),
  generate(),
  generate(),
  generate(),
  generate(),
  generate(),
  generate(),
  generate(),
  generate(),
  generate(),
];

Object.freeze(Colors);

export { Colors };
